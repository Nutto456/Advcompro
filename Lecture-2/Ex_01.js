function findPerimeter(length, width) {
    var perimeter = 2 * (length + width);
    return "findPerimeter(" + length + ", " + width + ") = " + perimeter;
  }
  
  // Example usage:
  console.log(findPerimeter(6, 7));
  console.log(findPerimeter(20, 10));
  console.log(findPerimeter(2, 9));
  